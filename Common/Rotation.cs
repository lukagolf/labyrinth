namespace Common
{
  /// <summary>
  /// Represents the number of degrees to rotate the spare tile in clockwise direction
  /// </summary>
  public enum Rotation
  {
    Zero = 0,
    Ninety = 90,
    OneHundredEighty = 180,
    TwoHundredSeventy = 270,
  }
}